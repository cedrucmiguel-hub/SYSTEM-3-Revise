# Legacy Services Note

The previous Fastify-based service folders were removed from the active source tree after the required API contracts were implemented in the NestJS backend.

The active backend runtime is now the NestJS application in `src/backend`.
